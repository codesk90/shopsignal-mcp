# ShopSignal v0.2 Backlog

## MCP Registry / PulseMCP

- Investigate whether Apify offers a path to register hosted MCP actors in the modelcontextprotocol/registry directly (without requiring devs to publish a duplicate npm package). If they don't, this is worth raising in Apify's Discord #mcp channel.
- PulseMCP auto-ingests from the registry weekly — we get PulseMCP listing for free when/if the registry path opens up.

## Smithery

- Stuck: Apify's MCP endpoint advertises OAuth in server metadata. Smithery detects this and loops on OAuth instead of using the Bearer header parameter. Need to either:
  - File issue with Smithery linking Apify's endpoint
  - Check if other Apify-hosted actors have successfully listed on Smithery and copy their approach
  - Contact Smithery support directly
- Confirmed: Smithery has no Bearer prefix templating. x-to/x-from only relocates raw values to headers — cannot prepend "Bearer ". No workaround in current UI.

## Glama

- Connector unhealthy: root cause confirmed — Glama's health checker sends `Accept: application/json` only. Our server requires `Accept: application/json, text/event-stream` and returns HTTP 406 otherwise.
- Option A: Fix server to fall back to plain JSON when SSE not in Accept header (actor code change).
- Option B: Reply to ticket #98672499 asking Glama to add `text/event-stream` to their checker's Accept header.
- Ticket #98672499 open, auto-ack received, no human reply yet.

## Stores

- 9 seed-list stores returning 404 (Vuori, Buck Mason, Princess Polly, Alo Yoga, Briogeo, Staud, Mytopicals, For Love & Lemons, Vuori Clothing). Wait for 7 days of cron data before investigating — some may resolve transiently.
- Skims: passes platform detection but returns empty catalog (Shopify Plus restriction). Investigate v2 bypass.
